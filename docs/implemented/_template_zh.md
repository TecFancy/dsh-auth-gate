# <名称> — <一句话>（实施规格）

> **状态**: proposed / in progress / implemented
> **适用范围**: 基于哪个里程碑（M0–M5）/ 覆盖哪些模块
> **来源**: 日期 + 验证实例 + 出处（review / ADR / plan 章节）

## 1. 背景

为什么存在：要补的缺口、绕开的约束、修掉的失败模式。

## 2. 行为契约

| 场景 | 行为 |
| ---- | ---- |
| ...  | ...  |

fail-open / fail-closed 的适用范围必须写清楚。

## 3. 冻结设计决策

- **D-<名称>-1**: 决策 + 理由（每条一行；有对应 ADR 时引用）。

## 4. 部署注意

拓扑要求、与反代模式的关系、运营卫生（日志 / token / cookie）。

## 5. 测试

- 单元：文件清单 + 各覆盖什么。
- 集成：真栈用例（锁装配边，不只是 hand-mounted fake）。

## 6. 变更记录

| commit   | 内容 |
| -------- | ---- |
| `<hash>` | ...  |
